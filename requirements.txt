# Core dependencies
# Note: vLLM version should match the version you're using
# The plugin is designed to work with vLLM v1 architecture (VLLM_USE_V1=1)
vllm>=0.7.0
transformers>=4.40.0

# TT-specific dependencies
# ttnn should be installed separately based on your Tenstorrent hardware setup
# torch version should match vLLM's requirements (typically provided by vLLM)

# Development dependencies (optional)
# pytest>=7.0.0
# black>=23.0.0
# ruff>=0.1.0

